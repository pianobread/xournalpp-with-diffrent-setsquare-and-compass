name: Build My Windows Xournal++

on:
  workflow_dispatch:

jobs:
  build:
    uses: ./.github/workflows/create-installers.yml
    with:
      windows_builds: |
        [
          {
            "runner": "windows-2022",
            "displayed_name": "Windows",
            "msystem": "mingw64",
            "msys_package_env": "x86_64",
            "build_portable": "true"
          }
        ]
